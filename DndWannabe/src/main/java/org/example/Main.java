package org.example;
import java.util.ArrayList;
import java.util.List;

// Enum for different scene types
enum SceneType {
    EUROPEAN, ASIAN, AFRICAN
}

// Enum for different character types
enum CharacterType {
    HUMAN, ELF, DRAGON, SKELETON
}

// Enum for different house materials
enum HouseMaterial {
    STONE, BAMBOO, ADOBE
}